const boxElement = document.getElementById("box");

boxElement.addEventListener("mouseout", function () {
  alert(
    "you left the safe zone 🙅‍♀️ (mouse out event is working properly)"
  );
});
